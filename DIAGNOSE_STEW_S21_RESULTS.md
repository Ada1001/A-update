# STEW 21号目标被试：固定权重源域统计诊断结论

输入：`diagnose_stew_s21.zip`。包含一个模型 `ms_tgc_spddsbn`、一个 LOSO 折，
目标被试21，源训练被试37人，源窗口11100，目标窗口300。绘图平衡样本600。

## 实验有效性

audit记录权重及非源域统计不变、checkpoint SHA256不变、目标logits最大绝对变化0。
148个源域统计键发生变化。独立读取预测与特征文件确认样本ID一致、所有保存特征有限，
目标210维表示及128维分类输入前后最大差异均为0，目标混淆矩阵完全相同。
没有原始checkpoint，无法独立重算其SHA256或逐项验证权重；这部分依据运行时审计。

## 分类结果

| 分区 | saved BAcc | source_refit BAcc | 差异 |
|---|---:|---:|---:|
| 源训练窗口 | 62.8919% | 85.1982% | +22.3063个百分点 |
| 目标测试窗口 | 84% | 84% | 0 |

源macro F1从58.6239%提高到85.1982%。37个源被试中36个BAcc提高，
12号从78.33%降到74%，因此不能声称所有被试都提高。
源窗口3251个错误被纠正，775个原正确预测变错，净增加2476个正确预测。

混淆矩阵按真实Low/High为行，预测Low/High为列：

```
saved/source          refit/source         target/both
5273   277            4733   817            131   19
3842  1708             826  4724             29  121
```

原源域预测明显偏向Low，重估后两类召回更均衡。目标仍有48个错误，
本次诊断没有提升目标测试性能，不能将源训练恢复作为新的泛化提升。

## 高维特征

| 位置 | 条件 | 域差异 | 类别中心距离 | 类别/域分离比 | 混合Fisher |
|---|---|---:|---:|---:|---:|
| 210维表示 | saved | .239926 | .086871 | .362076 | .017700 |
| 210维表示 | source_refit | .001818 | .004144 | 2.279050 | .323609 |
| 128维分类输入 | saved | 1.625182 | 1.315699 | .809570 | .495405 |
| 128维分类输入 | source_refit | .632338 | 2.430058 | 3.842976 | 1.012516 |

210维源域类内方差从.994422变成约.000057，目标一直约.000053。
原源域尺度明显大于目标；重估后尺度相近。由于沿用saved源域标准化器，
不能把.001818解释为没有域差异，或者把类别距离变小直接判为塌缩。
应综合Fisher、预测和非零类内方差。源域210维Fisher从.022436到.294876，
目标Fisher保持.488199。目标没有被变换成更容易分类的特征。

128维位置源域Fisher从.236641到1.087502，目标保持1.097166。
说明后续LayerNorm/投影/GELU提供了更直接的分类表示，原210维 pooled
Euclidean中心距离不能单独代表模型判别能力。不同位置的绝对距离不能直接比较，
每个位置使用自己独立拟合的saved源域scaler。

## 降维图

- 原210维表示在有/无PCA的UMAP中都存在源域分群与目标挤压，因此PCA30不是唯一原因。
- 原表示的联合t-SNE中目标已有类别结构，但源、目标总体仍分离；原UMAP的目标小团不等于高维塌缩。
- 源域refit后，UMAP里的孤立源群消失，目标点分散进入源域区域，但类别仍有明显重叠。
- refit后的128维t-SNE显示更清楚的蓝绿类别方向及同类源/目标混合，并非完美分离。
- 目标高维特征完全相同仍可显示不同二维坐标，因为投影重新拟合，且源输入改变。
  因此不得把左右图解释为目标特征因refit而发生移动。

## 原因与结论边界

在该固定模型和数据划分下，源域统计重估是唯一干预，源推理显著恢复，
因此有强证据表明保存的源域BN统计是本折异常的主要近因。
代码中源域使用训练期EMA推理统计，验证/目标域用选定权重重新估计，
这种不对称得到本次实验支持。具体为何EMA失准（特征漂移、增强、小域批次、
动量等）尚未由实验区分。
37个源被试共享同一个checkpoint，不是37个独立LOSO模型；不能替代48折验证。
也不能据此解释TSMNet的eigh崩溃：本包没有TSMNet结果和失败时矩阵。

## 后续顺序

1. 完整模型执行 `--subjects all`，先只提取指标，不为48折全部做UMAP/t-SNE。
2. 汇总每折不变量、源BAcc差异、目标预测变化，以及两个位置的指标变化；报告失败折和变差折。
3. 其他方法按一致规则做对照：EuDSBN用源训练域refit，Mean-CE/全局SPDBN不伪造分域refit。
4. 保留原图及原checkpoint。若所有折支持源统计问题，制定显式source-calibrated分析协议，
   将归一化统计校准与原目标适配分开记录，再产出新的比较图。诊断不直接覆盖论文主图。
5. 主表示图仍报告规定的提取位置，128维图作为分类特征补充；TSMNet无同构128维头，须如实标注。
6. 暂不调随机种子、UMAP参数来追求漂亮图，也无需先重训。训练期组批/动量调整需要独立实验，
   使用源验证数据选参数，不能据目标测试标签调参。

全折命令见本次回复。压缩回传CSV/JSON即可完成第一轮跨折统计，无需打包全部高维特征。
