# Fig7 fixed v2 结果复核

输入：项目根目录 `fig7_fixed_v2.zip`、`outputs/master_summary.csv`，以及当前训练和图7代码。服务器历史训练代码不在压缩包中，不能从当前代码倒推出旧版本的行为。

## 1. 运行完整性

压缩包 CRC 检查通过，已包含正式 PDF/PNG。六种模型、STEW 48 折与 EEGMAT 36 折，共504个计时记录、504000次正式推理。逐一核对延迟数组 SHA256、1000次长度、有限正值以及汇总表中的 BAcc/延迟中位数，均通过。所有折记录的 FP32 与原生预测不一致比例为0。输入审计无 error，但旧日志的来源和缺失训练耗时仍是 warning，不能解释为所有历史协议都已证明一致。

硬件：NVIDIA GeForce RTX 4090（不是4090D），PyTorch 2.8.0+cu128，batch=1、threads=1、warmup=100、repeats=1000、FP32 推理副本。TSMNet/AGMNet 数字属于显式 FP32 移植，不是原生 CPU/FP64 SPD 路径的耗时。BF-GCN 特征预处理、所有方法数据加载和域统计重估不计入延迟。

|方法|STEW BAcc (%)|STEW 延迟 (ms)|EEGMAT BAcc (%)|EEGMAT 延迟 (ms)|
|---|---:|---:|---:|---:|
|EEGNet|69.39|0.146|61.94|0.143|
|EEG-Conformer|70.53|1.755|63.31|1.752|
|BF-GCN|70.14|1.169|65.74|1.232|
|MDTN-GMDA|71.96|0.554|65.67|0.555|
|TSMNet|79.22|2.069|77.25|2.073|
|AGMNet|82.90|3.892|69.02|3.883|

STEW 的 AGMNet 比 TSMNet 高3.67个百分点，但延迟约1.88倍；EEGMAT 则低8.23个百分点且更慢。本次六方法比较中，AGMNet 两个数据集均最慢；不能写成全面更优或最高计算效率。AGMNet 的参数少于 EEG-Conformer 不等于延迟更低，图与 SPD 算子、张量规模和调度都影响实际时间。上述差值是描述性结果，未据此建立统计显著性。

## 2. 为什么(a)(b)不适合支持最终泛化/效率结论

当前(a)(b)画的是逐 epoch 源域验证 BAcc，(c)(d)是最佳验证 loss checkpoint 在最终目标被试上的 BAcc。这两个评估对象和统计状态不同。

根据各折最小 val_loss 对应的真实日志，TSMNet 的平均验证 BAcc 为 STEW 63.76%、EEGMAT 56.55%，最终目标 BAcc 为79.22%、77.25%。AGMNet 对应验证 BAcc 为81.63%、77.46%，最终目标 BAcc 为82.90%、69.02%。因此验证曲线的相对排序并不等于最终目标排序。

当前 `training.py` 的流程是：训练一个 epoch →（满足条件时，临时用验证输入重估该域统计）→评估验证集 →恢复统计 →按验证 loss 选权重。训练结束恢复所选权重，评估验证集，再用无标签目标数据重估统计，最后测试。目标重估发生在验证之后，不能直接断言它造成前面的验证准确率下降。旧 TSMNet 总表中 `val_stat_refit` 缺失；AGMNet 明确为 true。这支持检查评估流程不一致的必要性，但缺失不等于 false，亦不能证明唯一原因。

其他可能因素包括验证被试难度、域统计漂移、训练过拟合、验证与目标域差异。排查应固定权重，重建原始划分与归一化，在模型副本上比较验证统计重估前后 BAcc/loss，确认仅验证域统计变化，目标预测不变。此项诊断可以支持机制解释，不能恢复所有历史 epoch 的曲线。只有最终 checkpoint 时，不能把最终测试准确率复制到每个 epoch。

E95 是达到各折自身最高验证 BAcc 的95%的首个 epoch。EEGMAT TSMNet 的 E95 中位数为1，同时早停 epoch 中位数为9：低峰值很容易达到，不能解释为快速学到了高质量表征。各 epoch 保留的折也不同（至少80%覆盖），后段均值可能受退出折影响。旧日志除补跑 MDTN 外缺少真实 elapsed time，不能用 epoch×估计时间画训练耗时曲线。

## 3. 推荐正文和补充材料的分工

正文更建议改为“Predictive performance and computational cost”：

- (a)(b)：分别为 STEW/EEGMAT，最终目标 BAcc 对参数量或模型存储量。
- (c)(d)：最终目标 BAcc 对实测推理延迟。
- 若坚持十方法全部同一版面，(a)(b)用最终 BAcc 点图及折间 SD 更清楚；SD需明确是被试差异而非置信区间。模型存储量/参数量另表列出。
- SVM 没有可直接等同神经网络权重数的通用参数量，也没有同等 GPU FP32 forward。可在准确率面板纳入 SVM，在独立 CPU 面板/表给出它的实测延迟和模型大小，不能把 CPU SVM 和 GPU 神经网络混称同硬件内核比较。
- 历史验证曲线可作为补充诊断，明确标注验证统计协议未完全核实。不要单纯因 TSMNet 曲线低而改指标。
- 若论文必须讲 learning efficiency，应另做预先固定预算的实验；用一致的验证流程画 BAcc 对实测训练时间，可另标域适配耗时。目标测试标签不得参与早停、超参或预算选择。现有最终 checkpoint 和旧 CSV 不足以补造这些曲线。

## 4. 十方法现有记录

|方法|STEW LOSO|EEGMAT LOSO|
|---|---|---|
|SVM|总表未发现|总表未发现|
|EEGNet|48折；本次计时完成|36折；本次计时完成|
|BiLSTM|总表48折|总表36折|
|TAHAG|总表48折|总表36折|
|LSCCN|总表48折|总表未发现|
|EEG-Conformer|48折；本次计时完成|36折；本次计时完成|
|BF-GCN|48折；本次计时完成|36折；本次计时完成|
|MDTN-GMDA|补跑48折；本次计时完成|补跑36折；本次计时完成|
|TSMNet|48折；本次计时完成|36折；本次计时完成|
|AGMNet|48折；本次计时完成|36折；本次计时完成|

总表中的 SVM single_session 不能当 LOSO。旧 MDTN 另有 `mdtn-gmda`（连字符）记录，STEW 61.23%、EEGMAT 62.08%，与本次补跑不同；不能把两次运行按显示名混在一起。旧 AGMNet 在同一路径有多个历史记录，不能选最高的一行配当前已覆盖的 checkpoint。

当前图7计时适配器只实现六种方法；给 METHODS 添加名称不足以扩展十方法。BiLSTM、TAHAG、LSCCN需要独立核对模型重建/前处理/预测接口，尤其 LSCCN 验证集所选 decision_threshold 必须恢复，不能默认 argmax。SVM需 joblib 和 CPU 计时协议。

新增 `analysis/fig7_baseline_inventory.py` 只读扫描总表及指定根目录的 summary.csv。检查标准被试集合、BAcc、各折 checkpoint 文件存在性，发现没有写入总表的运行也会列出。FILES_PRESENT不意味着权重严格加载、版本兼容或计时协议已经验证。当前本地扫描不能替代服务器磁盘检查。

下一步先在服务器运行 inventory，确认缺项，再补缺失实验/适配器。无需重跑已经完成的六模型计时，也不要修改旧实验的记录来迁就绘图。
